first in database there should be a user
one admin user can create another admin, oprator
so first admin is needed in the database compulsary to authenticate

//make some changes and commit again to redeploy or you can also use vercel cli to redeploy
//changing version of nodejs from 20 to 18 solves the 500 internal server error in vercel
